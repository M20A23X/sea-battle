import { ISpecsConfig } from '../types/specs';

const Default: ISpecsConfig = {
    specs: {
        dataAmount: 20,
        hookTimeoutMs: 50000,
        connectionCheckIntervalMs: 200
    }
};

export { Default };
